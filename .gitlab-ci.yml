image: docker

services:
  - docker:dind

stages:
  - build
  - test

before_script:
  - docker version
  - apk add --no-cache docker-compose
  - docker-compose version
  - docker login -u gitlab-ci-token -p $REGISTRY_TOKEN $REGISTRY

after_script:
  - docker-compose -f docker-compose.ci.yml down
  - docker-compose -f docker-compose.ci.yml ps


build:
  stage: build
  script:
    - echo "Building ..."
    - docker-compose -f docker-compose.ci.yml build
    - docker-compose -f docker-compose.ci.yml push

test_core:
  stage: test
  needs:
    - build
  script:
    - echo "Testing ..."
    - docker-compose -f docker-compose.ci.yml pull
    - docker-compose -f docker-compose.ci.yml run web sh -c "pipenv run python manage.py makemigrations && pipenv run python manage.py migrate && pipenv run python manage.py test the_social_network -v 2"