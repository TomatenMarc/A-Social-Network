image: docker

services:
  - docker:stable-dind

stages:
  - build
  - test

before_script:
  - docker info
  - apk add --no-cache docker-compose
  - docker-compose info

build:
  stage: build
  script:
    - echo "Building ..."

test:
  stage: test
  script:
    - echo "Testing ..."
